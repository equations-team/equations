#!/usr/bin/python

import cgi, cgitb

# This will create field storage or database access
storage = cgi.FieldStorage()

# Then we get data from the forms

# Currently this is commented out because the forms do not get filled yet. 
#=======================================
# username = form.getvalue('username')
# password = form.getvalue('password')
#========================================
